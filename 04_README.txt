04_train_non_ar_asr.py は、pyhon のプログラムです。

#python 04_train_non_ar_tts.py

で実行してください。700epochs 実行するように設定してあります。この学習に GPU ありで 1日程度かかります。
実行途中に、fig フォルダに 20 epochs ごとに mel スペクトログラムが、100 epochs ごとに pt ファイルが保存されます。
